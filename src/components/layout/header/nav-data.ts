/**
 * The header renders the unified navigation DTO. Canonical definition lives in
 * `@/types/navigation`; re-exported here so header components keep their import.
 */
export type { NavNode, NavImage } from "@/types/navigation";
